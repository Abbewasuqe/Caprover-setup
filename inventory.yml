all:
  children:
    caprover-server:
      hosts:
        [Enter IP Adress]:
          ansible_user: ubuntu
          ansible_ssh_private_key_file: my-keypair

    caprover-worker:
      hosts:
        [Enter IP Adress]:
          ansible_user: ubuntu
          ansible_ssh_private_key_file: my-keypair
